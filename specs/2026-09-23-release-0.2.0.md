---
status: implemented
project: scs
project-root: /Users/leonardo/Repositories/scs
created: 2026-09-23
updated: 2026-09-23
owner: release
decision:
supersedes:
superseded-by:
implementation:
  commits: [a60106e, 4fe4581]
  pull-request:
---

# Release SCS 0.2.0

## Outcome

Publish the current unreleased changes as SCS `0.2.0` through the established
GitHub Release workflow. The version signals that the agent-facing MCP read
inventory changes from seven specialized tools to `query_code`.

## Scope and contracts

- Set Python metadata, runtime, Rust workspace dependencies, and both lockfiles
  to `0.2.0`. Move the unreleased changelog entries into a dated `0.2.0`
  section. Update the README quick start and explain the current Laya query
  path.
- Release the exact `main` commit that passes local verification and GitHub CI.
- Publish the existing macOS and Linux wheels, source distribution, constraints,
  installer, SBOM, checksums, and provenance through the tag workflow.
- Keep the query contract in
  [unified-query-orchestration.md](2026-09-22-unified-query-orchestration.md)
  and the caller migration in
  [query-code-migration.md](../docs/query-code-migration.md) authoritative.
  This release adds no further API or persisted-data change.

## Acceptance criteria

- Every version identity and generated package lock agrees with `v0.2.0`.
- The published release comes from the verified `main` commit, and macOS,
  Linux, and supply-chain CI pass before the annotated tag is pushed.
- The tag workflow passes, and the public release provides every documented
  asset with matching `SHA256SUMS` entries.
- A clean installation of a published wheel reports `0.2.0` and exposes the
  five-tool MCP inventory described by the migration guide.
- The README explains the query flow, Laya's local role, and source-checkout
  setup with an example that matches the public tool contract.

## Verification plan

- Run the existing release identity test before and after the bump, the version
  checker, and `just verify` locally.
- Inspect the GitHub CI run for the release commit before tagging; inspect the
  tag workflow and published assets after tagging.
- Verify downloaded assets against checksums and run a clean installed-wheel
  smoke test on an available platform. The release workflow also smoke-tests
  both wheels on hosted runners.

## Risks and recovery

The MCP change requires hard-coded callers to migrate. Existing persisted
indexes need no format migration. Before the tag, a failed gate blocks release.
After publication, assets and tags are immutable; correction uses a new version.
Installing the previous compatible release is the rollback path.

## Verification results

- Passed before the bump: `uv run --frozen pytest -q
  tests/unit/test_release_version.py` (2 tests).
- Passed after the bump: `python3 scripts/check-release-version.py v0.2.0`,
  `uv run --frozen pytest -q tests/unit/test_release_version.py` (2 tests),
  `cargo check --workspace --offline`, and `git diff --check`.
- Passed locally: `just verify` (Basedpyright and Ruff clean, 395 Python tests,
  106 Rust tests, plus the remaining required build and lint checks).
- README reviewed against the current query contract, Laya configuration,
  release installer, and pinned TSG tag; `git diff --check` passed. No automated
  test was added for the documentation edit.
- GitHub [main CI](https://github.com/leonardoventurini/scs/actions/runs/35890142465)
  passed supply-chain, macOS, and Linux jobs on `4fe4581`. Annotated tag
  `v0.2.0` resolves to that commit.
- GitHub [release workflow](https://github.com/leonardoventurini/scs/actions/runs/35891090243)
  passed validation, macOS and Linux wheel builds and smoke tests, source
  packaging, and publication. The public release contains both wheels, the
  source distribution, constraints, installer, SBOM, and `SHA256SUMS`.
- Downloaded every release asset and ran `shasum -a 256 -c SHA256SUMS`: all six
  payload files passed. `gh attestation verify` passed for the macOS wheel.
- Installed the published macOS wheel with its release constraints into an
  isolated `uv` tool directory. `scs version` and native import reported
  `0.2.0`; `scs --help` worked; the installed MCP server listed exactly the
  five documented tools.
- The public release notes now call out the breaking MCP change and link the
  migration guide. A separate manual Linux host installation was not run;
  the Linux release job performed a clean wheel smoke test.
