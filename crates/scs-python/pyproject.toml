[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "scs-native"
version = "0.1.0"
description = "Private Rust extension for SCS parsing and graph storage"
requires-python = ">=3.11"

[tool.maturin]
module-name = "_scs_native"
features = ["python"]
