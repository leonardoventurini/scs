[env]
# PyO3 0.23 predates CPython 3.14 metadata but uses the stable extension ABI
# exercised by SCS's native contract suite. Keep every PEP 517/Cargo invocation
# consistent instead of relying on a caller's shell environment.
PYO3_USE_ABI3_FORWARD_COMPATIBILITY = "1"
